/* eslint-disable no-unused-vars */
const filename = require("path").basename(__filename).split(".")[0];
module.exports = {
	run: (NECos, interaction) =>{

	},
	config: {
		adminOnly: false,
		forceEnabled: true,
		enabled: true,
		arguments: undefined,
		description: "Pong!"
	}
};